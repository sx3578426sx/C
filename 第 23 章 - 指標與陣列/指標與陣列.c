#include <stdio.h>

int main(){
    int v[5];  //有5個元素的int陣列，占據5個int大小的記憶體
    int *n; //一個int指標，佔據1個int * 大小的記憶體
    n = &v[0];
    //陣列型別可以隱性轉型成該陣列第一個元素記憶體位址的指標
    n = v   //相當於 n = &v[0]

    //指標*n 等於陣列v[0]
    n == &v[0];
    *n == v[0];
    //所以從指標位址設*n=0，等價於將陣列v[0]=0是一樣的
    *n = 0;
    v[0] = 0;

    //同理指標*(n+1)等於陣列v[1]
    n + 1 == &v[1];
    *(n+1) == v[1];
    //所以指標*(n+1) = 0，等價於陣列v[1]改成0
    *(n+1) = 0;
    v[1] = 0;
}
